export const metadata = { title: 'About' };

export default function AboutPage() {
  return (
    <section className="grid">
      <h1>About</h1>
      <div className="card copy">
        This project turns surprising time-gap comparisons into a searchable content site. The long-term model is to use
        free content for discovery and trust, then add personalized merchandise for birthdays, anniversaries, weddings,
        and other milestones.
      </div>
    </section>
  );
}
